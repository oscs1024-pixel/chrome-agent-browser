# 隐私声明（agent-browser）

## 原则

**agent-browser 不上传你的任何数据。** 它完全在你自己的浏览器和你自己的本地电脑之间工作，所有链路均在本地，不经过任何外部服务器。

## 数据流向与存储

- 扩展与「桥守护进程」通过 WebSocket 本地通信。桥进程运行在**你自己的电脑**上，只监听 `127.0.0.1`（本机回环地址）。所有命令、页面内容、截图都只在这条本地链路上传输。
- 审计日志记录仅写入**本地磁盘**（`~/.agent-browser/audit.jsonl`）。
- 绝无任何远程服务器上报、分析 SDK、广告或跟踪代码。

## 敏感信息防护

- 扩展内置凭据脱敏算法，日志中出现的**密码、Token、2FA 恢复码等敏感信息自动脱敏**后再落盘。
- 浏览器里的登录态（Cookie、Local Storage 等）始终驻留在浏览器进程内，仅通过扩展操作页面，凭据本身不会被导出离开浏览器。

## 用户控制权

- 所有数据完全保存在本地磁盘，直接清理 `~/.agent-browser/` 即可移除全部运行时产生的数据。
